version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: wpcc-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-SecretPassword123!}
      POSTGRES_DB: ${POSTGRES_DB:-wp_control_center}
    # Bind to loopback only — never expose the DB on a non-local interface.
    ports:
      - "127.0.0.1:5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

  redis:
    image: redis:7-alpine
    container_name: wpcc-redis
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD:-}"]
    # If REDIS_PASSWORD is empty/unset, Redis starts without auth (safe for
    # local-only dev). In production set a strong password.
    # Bind to loopback only:
    ports:
      - "127.0.0.1:6380:6379"
    volumes:
      - redis_data:/data
    restart: always

volumes:
  postgres_data:
  redis_data:
